import { SuperHeaderctrlDirective } from './superadminheaderctrl.directive';

describe('HeaderctrlDirective', () => {
  it('should create an instance', () => {
    const directive = new SuperHeaderctrlDirective();
    expect(directive).toBeTruthy();
  });
});
